# 更新日志 (Changelog)

## 🎯 v1.2.0 - 项目重构与子模块整合 (2025-12-14)

### 🔧 项目结构优化

- **子模块整合**：
  - 添加 `kaggle-gemini3-writeups-explorer` 子模块 - Kaggle Writeups 浏览器 (Streamlit)
  - 添加 `prometheus-to-grafana-ai` 子模块 - Grafana Dashboard 生成器 (Gemini版)

- **文件清理**：
  - 移除冗余的本地数据目录
  - 清理旧版脚本文件
  - 统一数据存储到子模块中

- **文档更新**：
  - 更新 README.md 和 README-zh.md
  - 修正 GitHub 用户名链接
  - 更新项目架构图

---

## 🎯 v1.1.8 - API 配置管理功能 (2025-10-30)

### ✨ 新功能

#### 本地 API 配置管理（支持多配置保存和选择）

**目标**：允许用户保存多个 API 配置到浏览器本地，方便在不同环境间快速切换

**改进内容**：

**前端改动** (`frontend/index.html`, `frontend/app.js`, `frontend/style.css`)
- 新增配置管理功能：
  - 使用 localStorage 本地存储所有配置
  - 支持保存多个不同的 API 配置（OpenAI、MiniMax 等）
  - 配置下拉选择器，快速切换配置
  - 配置管理 Modal，查看和管理所有配置
- 新增配置操作：
  - **Save Current Config** - 保存当前输入的配置
  - **Manage Configurations** - 打开配置管理界面
  - **Add New Configuration** - 添加新配置
  - **Edit** - 编辑现有配置
  - **Set Default** - 设置默认配置（页面加载时自动应用）
  - **Use** - 使用配置（自动填充表单）
  - **Delete** - 删除配置（带确认提示）
- 安全特性：
  - API Key 脱敏显示（列表中只显示前8位和后4位）
  - 配置完全保存在浏览器本地，不上传服务器
  - 编辑时才显示完整的 API Key
- 美观的 UI：
  - Modal 弹窗设计
  - 配置卡片式布局
  - 默认配置蓝色高亮显示
  - 响应式设计，移动端友好

**使用流程**：
1. **保存配置**：输入 API Key/Base URL/Model → 点击 "Save Current Config" → 输入名称
2. **使用配置**：下拉框选择已保存的配置 → 表单自动填充
3. **管理配置**：点击 "Manage Configurations" → 编辑/删除/设置默认

**用户价值**：
- ✅ **方便切换**：在不同 API 提供商之间快速切换（OpenAI ↔ MiniMax）
- ✅ **多环境支持**：保存开发/测试/生产环境的配置
- ✅ **避免重复输入**：配置保存在本地，无需每次输入
- ✅ **安全可靠**：配置只保存在本地浏览器，不会泄露

**相关文件**：
- `frontend/app.js` - 配置管理逻辑（loadConfigs、saveConfigs、addConfig 等）
- `frontend/index.html` - 配置选择器和管理 Modal
- `frontend/style.css` - 配置管理界面样式

---

## 🎯 v1.1.7 - Metrics 预览功能 (2025-10-30)

### ✨ 新功能

#### 前端 Metrics 信息预览（第一步确认）

**目标**：在发送到后端前，先在前端展示 metrics 精简信息，节省带宽并提高用户体验

**改进内容**：

**前端改动** (`frontend/index.html`, `frontend/app.js`, `frontend/style.css`)
- 新增前端 Metrics 解析逻辑：
  - 提取 metric 名称、类型（counter、gauge、histogram 等）
  - 提取 HELP 说明信息
  - 提取所有 labels/tags
  - 统计每个 metric 的样本数
- 新增 "📊 Metrics Information" 展示区域：
  - 总览统计：Total Metrics / Unique Labels / Metric Types
  - Metrics 列表：每个 metric 显示名称、类型、help、labels、样本数
  - Common Labels：所有使用的 label 标签
  - Metric Types：按类型分类统计
- 美观的卡片式布局：
  - 每个 metric 卡片有类型徽章（带颜色区分）
  - Label 标签绿色高亮显示
  - Hover 效果和滚动支持
- 新增操作按钮：
  - "➡️ Next: AI Analysis" - 确认后才调用后端 API
  - "← Back to Edit" - 返回编辑 metrics

**新的三步流程**：
1. **Step 1: 输入 & 预览**
   - 粘贴 metrics → 点击 "Generate Dashboard"
   - 前端解析并展示 metrics 信息（无后端调用）
   - 用户查看并确认 metrics 信息

2. **Step 2: AI 分析**
   - 点击 "Next: AI Analysis"
   - 调用后端 `/api/analyze-metrics`
   - 展示 AI 计划的 panel plans

3. **Step 3: 选择 & 生成**
   - 选择要生成的 panels
   - 点击 "Generate Selected Panels"
   - 生成 dashboard JSON

**技术优势**：
- ✅ **节省带宽**：用户确认后才发送数据到后端
- ✅ **提前验证**：在前端就能发现 metrics 格式问题
- ✅ **透明展示**：用户清楚看到有哪些 metrics 和 labels
- ✅ **快速响应**：前端解析瞬间完成，无需等待

**展示信息**：
- Metric 名称和类型（counter/gauge/histogram/summary）
- HELP 描述信息
- Labels/Tags 列表
- 每个 metric 的样本数量
- 所有唯一 labels 汇总
- 按类型分类统计

**相关文件**：
- `frontend/app.js` - 新增 `parseMetricsInfo()` 解析函数
- `frontend/index.html` - 新增 metrics 信息展示区域
- `frontend/style.css` - 美化样式（metric 卡片、类型徽章、label 标签等）

---

## 🎯 v1.1.6 - Panel 选择功能 (2025-10-30)

### ✨ 新功能

#### 用户可选择生成哪些 Panels

**目标**：增加用户控制权，允许选择性生成需要的 panels

**改进内容**：

**后端改动** (`backend/src/dashboardGenerator.js`, `backend/src/server.js`)
- 新增 `analyzeMetrics()` 函数：只执行 Stage 1 分析，返回 panel plans
- 新增 `generatePanelsFromPlans()` 函数：接收选中的 plans，执行 Stage 2 生成
- 新增 API 端点：
  - `POST /api/analyze-metrics` - Stage 1: 分析指标并返回 panel plans
  - `POST /api/generate-panels` - Stage 2: 根据选中的 plans 生成 dashboard
- 保留原有 `POST /api/generate-dashboard` 端点用于向后兼容（一步生成）

**前端改动** (`frontend/index.html`, `frontend/app.js`, `frontend/style.css`)
- 每个 panel plan 卡片添加复选框，默认全选
- 新增选择控制区域：
  - 显示 "X of Y panels selected" 统计信息
  - "Select All" / "Deselect All" 快捷按钮
- 新增操作按钮：
  - "✨ Generate Selected Panels" - 生成选中的 panels
  - "← Start Over" - 取消并重新开始
- 卡片点击切换选择状态
- 选中状态视觉反馈（高亮边框和背景色）
- 没有选中任何 panel 时禁用生成按钮

**用户流程**：
1. **分析阶段**：输入 metrics → 点击 "Generate Dashboard" → AI 分析并显示计划
2. **选择阶段**：查看所有计划的 panels → 勾选/取消勾选需要的 panels
3. **生成阶段**：点击 "Generate Selected Panels" → 只生成选中的 panels
4. **结果展示**：下载生成的 dashboard JSON

**用户价值**：
- ✅ **灵活控制**：可以只生成需要的 panels，节省时间
- ✅ **快速预览**：先看计划再决定，避免生成无用的 panels
- ✅ **分批生成**：可以先生成一部分，测试后再生成其他
- ✅ **成本节省**：对于大量 metrics，可以只生成关键的 panels

**技术亮点**：
- 两步 API 设计，前后端状态管理清晰
- 复选框状态与视觉效果同步
- 响应式设计，移动端友好
- 保持向后兼容，不影响现有用户

**相关文件**：
- `backend/src/dashboardGenerator.js` - 新增两个独立函数
- `backend/src/server.js` - 新增两个 API 端点
- `frontend/index.html` - 选择控制 UI
- `frontend/app.js` - 两步生成逻辑
- `frontend/style.css` - 复选框和选择状态样式

---

## 🎯 v1.1.5 - Panel Plans 可视化展示 (2025-10-29)

### ✨ 新功能

#### 在页面显示第一步分析结果（Panel Plans）

**目标**：增加透明度，让用户看到 AI 计划生成哪些 Panels

**改进内容**：

**后端改动** (`backend/src/dashboardGenerator.js`, `backend/src/server.js`)
- `generateDashboard` 函数现在返回完整对象：`{ dashboard, panelPlans, metadata }`
- 在 API 响应中包含 `panelPlans` 和详细的 `metadata`
- 新增以下统计信息：
  - `totalPanelsPlanned` - 计划生成的 panel 数量
  - `successfulPanels` - 成功生成的 panel 数量
  - `failedPanels` - 失败的 panel 数量
  - `failedPanelsList` - 失败 panel 的详细信息

**前端改动** (`frontend/index.html`, `frontend/app.js`, `frontend/style.css`)
- 新增 "Planned Panels" 展示区域
- 每个 panel plan 显示：
  - Panel 标题和图标（根据可视化类型）
  - 描述信息
  - 可视化类型
  - 使用的指标列表
  - PromQL 查询提示（如果有）
- 美观的卡片式布局，支持 hover 效果
- 在结果中显示计划/成功/失败的 panel 数量
- 如果有失败的 panel，显示警告信息

**用户体验提升**：
- ✅ 用户可以在生成完成后看到 AI 的分析过程
- ✅ 清晰了解每个 panel 的用途和使用的指标
- ✅ 如果有失败的 panel，能看到警告并继续使用成功的部分
- ✅ 更好的进度反馈和结果展示

**相关文件**：
- `backend/src/dashboardGenerator.js`
- `backend/src/server.js`
- `frontend/index.html`
- `frontend/app.js`
- `frontend/style.css`

---

## 🎯 v1.1.4 - Prompt 智能优化 (2025-10-29)

### ✨ 新功能

#### 1. 在 Prompt 中添加 Grafana Schema 参考

**目标**：帮助 LLM 更准确地理解 Grafana 数据结构，生成更符合规范的配置

**改进内容**：

**System Message 增强**
- 添加 Dashboard 结构知识（panels, templating, time, schemaVersion）
- 添加 Panel 对象必需字段说明
- 添加 Template Variables 使用说明
- 强调严格遵循 Grafana schema

**Panel Generation Prompt 增强**
- 添加详细的 Panel Schema 参考
- 说明每个字段的作用和类型
- 强调 24 列网格布局系统
- 明确 fieldConfig.overrides 应为空数组

#### 2. 添加 PromQL 语法规则 ⭐ 新增

**问题**: LLM 可能生成错误的 PromQL 语法，如 `rate(metric[5m]) by (label)`

**解决方案**: 在 Prompt 中添加详细的 PromQL 语法规则

**添加的规则**:
1. **聚合操作符必须包裹表达式**
   - ✅ 正确: `sum(rate(metric[5m])) by (label)`
   - ❌ 错误: `rate(metric[5m]) by (label)`

2. **常见聚合操作符**: sum, avg, max, min, count, stddev, topk, bottomk

3. **Rate 函数使用**:
   - rate(counter[5m]) - 每秒平均速率
   - irate(counter[5m]) - 瞬时速率
   - increase(counter[5m]) - 区间增长量

4. **Histogram 分位数**:
   - histogram_quantile(0.95, sum(rate(metric_bucket[5m])) by (le))

5. **常见模式示例**:
   - 请求速率: `sum(rate(requests_total[5m])) by (method, status)`
   - 错误率: `sum(rate(requests_total{status=~"5.."}[5m])) by (method)`
   - 延迟 p95: `histogram_quantile(0.95, sum(rate(latency_bucket[5m])) by (le))`

### 📝 修改的文件

**backend/src/prompts.js**:
- `getSystemMessage()` - 添加 Grafana 结构知识 + PromQL 语法规则
- `getPanelGenerationPrompt()` - 添加 Panel Schema 参考 + PromQL 语法规则
- Requirements - 强调正确的 PromQL 聚合语法

### 📚 新增文档

- ✅ `Prompt优化说明.md` - Prompt 优化说明和完整 Schema 参考
- ✅ `PromQL语法规则.md` - 完整的 PromQL 语法规则和最佳实践
- ✅ 更新 `常见Grafana错误修复.md` - 添加 PromQL 语法错误说明

### 🎯 预期效果

**减少的错误**：
- ❌ "byRefId not found" 错误
- ❌ "Invalid panel type" 错误  
- ❌ Schema 验证错误

**提升的质量**：
- ✅ 更准确的字段名
- ✅ 更完整的结构
- ✅ 更好的兼容性
- ✅ 更标准的 JSON

---

## 🔧 v1.1.3 - 数据源配置修复 (2025-10-29)

### 🐛 关键修复

#### 1. 修复 "datasource not found" 错误

**问题**：导入 dashboard 时提示 `datasource not found`

**原因**：硬编码了 datasource uid 为 "prometheus"，但用户的 Grafana 中可能没有这个 uid 的数据源

**解决方案**：
- ✅ 使用 `$datasource` 模板变量替代硬编码 uid
- ✅ 添加 datasource 选择器到 dashboard
- ✅ 导入后用户可以从下拉框选择自己的数据源
- ✅ 一次选择，所有 panel 自动生效

### 📝 修改的文件

**backend/src/prompts.js**:
- datasource uid 从 `"prometheus"` 改为 `"$datasource"`
- 添加要求：禁止使用硬编码 uid

**backend/src/dashboardGenerator.js**:
- 添加 datasource 模板变量配置
- 更新 dashboard 描述，提示用户选择数据源

#### 2. 修复 "byRefId not found" 错误

**问题**：Grafana 提示 `"byRefId" not found`

**原因**：LLM 生成的 panel 中使用了不存在的匹配器 `"byRefId"`

**解决方案**：
- ✅ 在 prompt 中明确禁止使用 `"byRefId"`
- ✅ 说明正确的匹配器是 `"byFrameRefID"`
- ✅ 推荐使用空的 `overrides: []` 避免兼容性问题

**修改**：
- 在 prompt 中添加匹配器使用说明
- 强调 overrides 应设置为空数组
- 列出所有有效的匹配器类型

### 📚 新增文档

- ✅ `数据源配置说明.md` - 详细的数据源配置文档
- ✅ `常见Grafana错误修复.md` - Grafana 错误排查和修复指南

### 💡 使用方式

1. 生成 dashboard
2. 导入到 Grafana
3. 在 dashboard 顶部的 "Data Source" 下拉框选择您的 Prometheus 数据源
4. 所有 panel 自动使用选中的数据源

### ✅ 优势

- **灵活性**：适用于任何 Grafana 实例，无需修改
- **易用性**：一次选择，全部生效
- **兼容性**：符合 Grafana 最佳实践

---

## 🚀 v1.1.2 - LLM 重试机制 (2025-10-29)

### ✨ 新功能

#### 自动重试机制
- ✅ LLM 调用失败时自动重试（默认最多 3 次）
- ✅ 指数退避策略：2s → 4s → 6s
- ✅ 详细的重试日志输出
- ✅ Stage 1（分析）和 Stage 2（生成）都支持重试

#### 增强的错误处理
- ✅ 单个 panel 失败不影响其他 panel 生成
- ✅ 记录并显示失败的 panel 信息
- ✅ 只要有至少 1 个 panel 成功就生成 dashboard
- ✅ 友好的进度和状态日志

#### 改进的 JSON 解析
- ✅ 处理完整和不完整的 `<think>` 标签
- ✅ 从 `<think>` 标签后提取 JSON
- ✅ 支持 markdown 代码块
- ✅ 智能提取 JSON 对象/数组
- ✅ 详细的错误日志（显示完整响应）

### 🔧 技术改进

**backend/src/llmService.js**:
- `callLLMForJSON()` 添加重试逻辑（maxRetries 参数）
- `parseJSONResponse()` 增强处理 `<think>` 标签
- 增加 `max_tokens` 从 4000 到 8000
- 更详细的错误日志输出

**backend/src/dashboardGenerator.js**:
- Stage 1 和 Stage 2 都使用重试机制
- 跟踪失败的 panel 并显示警告
- 改进的日志格式（使用分隔符）
- 只要有部分 panel 成功就继续

**backend/src/prompts.js**:
- 更严格的 system message
- 明确要求不使用 `<think>` 标签
- 明确要求不使用 markdown 代码块
- 强调只返回纯 JSON

### 📚 新增文档

- ✅ `重试机制说明.md` - 详细的重试机制文档

### 💡 使用场景

**问题**: LLM 返回包含 `<think>` 标签或格式错误  
**解决**: 自动重试 + 智能 JSON 提取

**问题**: 某个复杂 panel 生成失败  
**解决**: 跳过该 panel，继续生成其他 panel

**问题**: 网络临时中断  
**解决**: 自动重试，指数退避

### 📊 性能影响

- 成功情况：无额外时间
- 1 次重试：+2 秒
- 2 次重试：+6 秒
- 全部失败（3 次）：+12 秒

### 🎯 配置选项

可以自定义重试次数和等待时间，详见 `重试机制说明.md`

---

## 🔧 v1.1.1 - Dashboard 生成修复 (2025-10-29)

### 🐛 关键修复

#### Dashboard JSON 结构修复
根据实际 Grafana Dashboard 示例对比，修复了多个导致生成的 dashboard 无法正常导入的问题：

**1. Dashboard 结构包装**
- ❌ 修复前：生成 `{dashboard: {...}, folderId: 0}` 包装结构
- ✅ 修复后：直接生成 dashboard 对象，符合 Grafana 导入格式

**2. Datasource 配置**
- ❌ 修复前：Panel 和 Target 缺少 datasource 配置
- ✅ 修复后：每个 panel 和 target 都包含完整的 datasource 对象
```json
"datasource": {
  "type": "prometheus",
  "uid": "prometheus"
}
```

**3. FieldConfig 完整性**
- ❌ 修复前：fieldConfig.defaults.custom 为空对象
- ✅ 修复后：包含完整的配置（axisBorderShow, axisCenteredZero, drawStyle, fillOpacity, lineInterpolation, showPoints, stacking 等）

**4. Schema 版本更新**
- ❌ 修复前：schemaVersion 38
- ✅ 修复后：schemaVersion 41（匹配最新 Grafana 版本）

**5. Plugin 版本**
- ❌ 修复前：缺少 pluginVersion
- ✅ 修复后：添加 `"pluginVersion": "12.0.1"`

**6. Target 配置增强**
- ❌ 修复前：只有基本的 expr 和 refId
- ✅ 修复后：包含 format, intervalFactor, legendFormat, datasource

**7. Annotations 配置**
- ❌ 修复前：空数组
- ✅ 修复后：包含标准的 "Annotations & Alerts" 配置

**8. Options 配置**
- ❌ 修复前：配置不完整
- ✅ 修复后：包含 legend 和 tooltip 的完整配置

### 📝 修改的文件

**backend/src/dashboardGenerator.js**:
- 移除 dashboard 外层包装
- 更新 schemaVersion 38 → 41
- 添加标准注释配置
- 增强验证函数（检查 datasource）

**backend/src/prompts.js**:
- 完全重写 panel 生成 prompt
- 添加完整的 datasource 示例
- 添加完整的 fieldConfig.defaults.custom 配置
- 添加完整的 options 配置
- 添加 11 条详细要求说明

### 📚 新增文档

- ✅ `DASHBOARD_FIX.md` - 详细的修复说明和对比

### ✅ 验证

修复后的生成器现在会：
1. ✅ 生成符合 Grafana 导入格式的 JSON
2. ✅ 包含所有必需的 datasource 配置
3. ✅ 包含完整的 fieldConfig 配置
4. ✅ 使用最新的 schemaVersion (41)
5. ✅ 包含 pluginVersion
6. ✅ 自动验证 panel 的 datasource 配置

### 🎯 测试建议

生成 dashboard 后请验证：
1. JSON 结构与标准 Grafana dashboard 匹配
2. 可以成功导入到 Grafana
3. Panel 正确显示数据源
4. 图表样式正确应用

---

## 🎉 v1.1.0 - MiniMax API 支持 (2025-10-29)

### ✨ 新功能

#### 1. 支持多种 LLM API
- ✅ **OpenAI API** - GPT-4, GPT-3.5 等
- ✅ **MiniMax API** - MiniMax-M2, abab6.5s-chat 等
- ✅ **其他兼容 OpenAI 格式的 API**

#### 2. 灵活的配置方式

**Web 界面配置**（新增）：
- API Key 输入框（支持 OpenAI key 或 MiniMax JWT token）
- Custom API Base URL 输入框（可选）
- Model Name 输入框（可选）

**环境变量配置**（增强）：
```env
# 通用 LLM 配置（推荐用于 MiniMax）
LLM_API_KEY=your-api-key
LLM_API_BASE_URL=https://api.minimaxi.com/v1
LLM_MODEL=MiniMax-M2

# OpenAI 配置（向后兼容）
OPENAI_API_KEY=sk-xxx
OPENAI_MODEL=gpt-4-turbo-preview
```

### 🔧 技术改进

#### 后端更新

**llmService.js**:
- `createClient()` 支持自定义 `baseURL`
- `callLLM()` 新增 `baseURL` 参数
- `callLLMForJSON()` 支持自定义端点

**dashboardGenerator.js**:
- `generateDashboard()` 支持 `baseURL` 参数
- 添加日志输出显示使用的模型和端点

**server.js**:
- API 端点接收 `apiBaseURL` 和 `modelName` 参数
- 支持 `LLM_API_KEY` 和 `LLM_API_BASE_URL` 环境变量
- 灵活的配置优先级

#### 前端更新

**index.html**:
- 新增 "Custom API Base URL" 输入框
- 新增 "Model Name" 输入框
- 更新标签文案支持多种 API

**app.js**:
- 收集并发送 API 配置参数
- 支持可选的自定义配置

### 📚 文档更新

新增文档：
- ✅ `MINIMAX_GUIDE.md` - MiniMax 使用指南
- ✅ `CHANGELOG.md` - 本更新日志

更新文档：
- ✅ `.env.example` - 添加 MiniMax 配置示例
- ✅ README.md - 更新 API 说明

### 🎯 配置优先级

系统按以下顺序读取配置（优先级从高到低）：

1. **请求参数**（Web 界面输入）
   - `openaiApiKey`
   - `apiBaseURL`
   - `modelName`

2. **通用 LLM 环境变量**
   - `LLM_API_KEY`
   - `LLM_API_BASE_URL`
   - `LLM_MODEL`

3. **OpenAI 环境变量**（向后兼容）
   - `OPENAI_API_KEY`
   - `OPENAI_MODEL`

### 🔄 向后兼容

- ✅ 保持与 v1.0.0 完全兼容
- ✅ 现有的 OpenAI 配置继续工作
- ✅ 无需修改已有配置即可使用

### 📝 使用示例

#### MiniMax 配置示例

**方式 1 - Web 界面**：
```
API Key: eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCJ9...
Custom API Base URL: https://api.minimaxi.com/v1
Model Name: MiniMax-M2
```

**方式 2 - .env 文件**：
```env
LLM_API_KEY=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCJ9...
LLM_API_BASE_URL=https://api.minimaxi.com/v1
LLM_MODEL=MiniMax-M2
```

#### OpenAI 配置示例（保持不变）

```env
OPENAI_API_KEY=sk-xxx
OPENAI_MODEL=gpt-4-turbo-preview
```

---

## 📦 v1.0.0 - 初始版本 (2025-10-29)

### ✨ 核心功能

- ✅ Prometheus Metrics 文本解析
- ✅ AI 驱动的仪表盘生成
- ✅ 两阶段生成流程（分析 + 生成）
- ✅ Web 用户界面
- ✅ RESTful API
- ✅ 完整的文档

### 🎯 主要特性

#### 智能生成
- 自动识别监控模式（RED, USE）
- 生成准确的 PromQL 查询
- 选择合适的可视化类型
- 配置单位和图例

#### 用户体验
- 简洁的 Web 界面
- 实时进度显示
- JSON 预览和下载
- 友好的错误提示

#### 技术架构
- Node.js + Express 后端
- 原生 JavaScript 前端
- OpenAI GPT-4 集成
- 模块化代码结构

### 📚 文档

- README.md - 完整文档
- QUICKSTART.md - 快速开始
- 快速开始.md - 中文指南
- SETUP.md - 详细配置
- PROJECT_OVERVIEW.md - 项目概览

---

## 🔮 计划中的功能

### v1.2.0 (未来)
- [ ] 仪表盘模板（RED, USE, Golden Signals）
- [ ] 流式响应支持
- [ ] 进度百分比显示
- [ ] 更多 LLM 提供商支持

### v1.3.0 (未来)
- [ ] 直接导入到 Grafana（API 集成）
- [ ] 批量处理多个 metrics 源
- [ ] 自定义 PromQL 模板
- [ ] Dashboard 预览功能

---

## 📞 支持

如有问题或建议，请查看：
- [README.md](README.md) - 完整文档
- [MINIMAX_GUIDE.md](MINIMAX_GUIDE.md) - MiniMax 使用指南
- [QUICKSTART.md](QUICKSTART.md) - 快速开始指南


